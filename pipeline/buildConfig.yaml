apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: msr-fitness-config
spec:
  runPolicy: Serial
  source:
      git:
        ref: master
        uri: https://github.com/kplogesh/openshift
      type: Git
  strategy:
      dockerStrategy:
      type: Docker
  output:
    to:
      kind: ImageStreamTag
      name: msr-fitness:1.0.0
  triggers:
    - type: ConfigChange
